[project]
name = "token-exp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rana Gaber", email = "ranagaber046@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.1.0",
    "datasets>=5.0.1",
    "sentencepiece>=0.2.2",
    "torch>=2.13.0",
    "transformers>=5.15.0",
]

[project.scripts]
token-exp = "token_exp:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
